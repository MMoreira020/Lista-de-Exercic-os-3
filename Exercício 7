Faça um programa que leia 10 inteiros positivos, ignorando nao positivos, e imprima sua
media.


#include <stdio.h>

int main(){

   int i, media;
   media = 0;
   int num[10];

   printf ("Entre com 10 numeros positivos:\n");

   for (i=0;i<10;i++){

        printf ("Digite o %d numero inteiro:\n", i+1);
        scanf ("%d", &num[i]);

        media += num[i]/10;
   }
   if (num<=0){
        printf ("Numero nao positivo, entre com outro numero: %d", i+1);
        scanf ("%d", &num);
   }
    printf ("A media calculada eh: %d\n", media);


    return 0;
}
