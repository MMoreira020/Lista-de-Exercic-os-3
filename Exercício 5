Faça um programa que peça ao usuario para digitar 10 valores e some-os.


#include <stdio.h>

int main(){

    int i, soma;
    soma = 0;
    int num[10];

    printf ("Digite 10 numeros:\n");

    for (i=0; i<10; i++){
        printf ("Entre com o %d numero(s) inteiro(s):\n",i+1);
        scanf ("%d", &num[i]);

        soma +=num[i];
    }
    printf ("Soma = %d", soma);
    return 0;
}
